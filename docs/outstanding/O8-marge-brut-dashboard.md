# O8 — Marge Brut dashboard not deployed

> Detail file for ledger item **O8**. Read only when picking up this item. Back to the [ledger](../OUTSTANDING.md).

| | |
|---|---|
| **Status** | OPEN — mock is live on Oak & Vine; **mock→live conversion merged into `main`** (2026-07-29, fast-forward `aaabc5b..fb52ab3`; branch `worktree-margebrut-live` retained). **Turnover now live on UAT Gloucester** (Mews staging/DV deployed 2026-07-29 — see log). Remaining gates: **O5 `UOM_COST` inflation (confirmed present on Ibis, ~700×)** + **only one stocktake date** (can't compute month opening/closing) + run `DEPLOY.txt` (rework `01_provision_uat_org` to the 2 Ibis orgs) |
| **Area** | Dashboard / vis queries + Report DB |
| **Owner / decides** | Andy |
| **Next action** | ✅ **Both data gates now clear on Gloucester** — `UOM_COST` fixed/deployed 2026-07-29, and the 60-day backfill gives **2 stocktake dates** (31 May + 30 June), so month opening/closing consumption is computable. Rework `01_provision_uat_org.sql` (superseded by O19 — orgs already exist) and run `live/DEPLOY.txt` **from step 5**, validating on **Ibis Gloucester Road** (OrgID 21) |
| **Sources** | `docs/superpowers/specs/2026-07-10-marge-brut-live-design.md`, `docs/superpowers/plans/2026-07-10-marge-brut-live.md`, `ClaudeDevelopment/integrations/MargeBrut/live/`, `memory/marge-brut-dashboard.md` |

## ✅ Where this work lives (updated 2026-07-29)
**MERGED.** The mock→live build is now on **`main`** (local fast-forward `aaabc5b..fb52ab3`, 15 commits). ⚠️ **Not yet pushed to `origin/main`** — push still outstanding. The branch `worktree-margebrut-live` and its worktree at `.claude/worktrees/margebrut-live` are retained but no longer the source of truth. The `live/` scripts are at `ClaudeDevelopment/integrations/MargeBrut/live/` on `main` (they are absent from `feature/v1.0-baseline-refresh`, which branched earlier).

## Context
Two things exist under "Marge Brut":
1. **The mock** (2026-06-08) — literal-`VALUES` vis queries reproducing the Accor Oct-2025 spreadsheet, live on UAT org **The Oak & Vine** (OrgID 16). Dataset tokens `MargeBrut*`. Still in place as the Accor demo artifact; `ClaudeDevelopment/integrations/MargeBrut/02,03`.
2. **The mock→live conversion** (2026-07-10) — a real cost-of-sales dashboard fed by Mews turnover + Growyze consumption, on a **new dedicated UAT "Three Rocks Hotel" org** (Mews+Growyze only, so Consumption÷Turnover reconciles). Built on the branch above.

## Live design (settled, user-approved)
- Full cost-of-sales model; **real Growyze feed** for the hotel (fetcher dependency); **new dedicated UAT org**.
- Grouping: per-product `MICROSERVICE_NAME` MDM on **both** `SAT_PRODUCT` (Mews turnover) and `SAT_INVITEM` (Growyze stock) → 6 groups (`Food`/`Breakfast`/`Wines`/`Bottled Beer`/`Soft Drinks`/`Spirit`).
- New fact `presentation.F_MARGEBRUT_MONTH` (group×month) is the single computed source; the 9 `MargeBrut*` vis queries become thin reads. Manual inputs (provisions, staff-meal, comp%) in `reference.MARGEBRUT_MANUAL`.
- 8 scripts in `live/`: `01_provision_uat_org`, `10_f_margebrut_month_table`, `11_reference_margebrut_manual`, `12_group_mapping`, `13_f_margebrut_month_control`, `14_margebrut_vis_queries`, `15_report_config`, `99_verify` + `DEPLOY.txt`.

## Gated on (nothing deployed/live)
- (a) **Org provisioning + integration mapping** — now tracked as **O19**: two real **Ibis** hotel orgs (GUIDs from microservice Prod) mapped to Growyze + Mews. This supersedes the single "Three Rocks Hotel" org the live design assumed and the `01_provision_uat_org.sql` single-org step (which will need reworking to two orgs).
- (b) **Key Vault secrets + loads** — Integrations ledger **O10**: DL landing **DONE on UAT**. ⚠️ The earlier "DV loads completed" claim was **only true for Growyze** — the Mews half had no staging/DV path at all until 2026-07-29 (see log). **Now resolved on UAT:** Turnover is live on **Gloucester** (`F_LINEITEM_15MIN` 0 → 62 rows, £1,349.02 net). **Heathrow still has no Mews POS transactions** (dimensions only). **Test** pending (SQL DB paused) + **Prod** pending — both still need the Mews control-plane deploy (`91_deploy_mews_uat.ps1 -Environment TEST`).
- (c) **O5 `UOM_COST` — ✅ CLEARED 2026-07-29 (fixed, deployed and verified on UAT).** Was a unit mismatch: `ACTUAL_COUNT` in **ml/gr** against a cost priced **per pack/bottle** (Hendricks 1,190 ml × £23.24/bottle = £27,656 vs a true ≈£40; whole stocktake £762,277 across 151 lines). Fixed by dividing cost by pack size at staging — `UOM_COST = price / COALESCE(NULLIF(size,0),1)` — leaving the presentation layer's existing `/ conversion_factor` division to carry measure→base unit. **Result: £762,277.46 → £8,157.61**, 0 null costs, 0 stuck rows across all 4 Growyze orgs, and it held through the later 60-day backfill. Marge Brut's consumption-side figures are now trustworthy. Detail: [O5](O5-growyze-default-dashboards.md) progress log 2026-07-29. ⚠️ Residual to watch at go-live: two known Growyze catalogue errors (ROCKET WILD, CORONET WHITE SUGAR STICKS) still misstate individual items.
- (d) **Single stocktake date — LIKELY RESOLVED for Gloucester by the 60-day backfill (2026-07-29).** Integrations re-ran a **60-day** load (window 2026-05-30→07-28): Gloucester Growyze DL now holds **8 stocktake reports** (was 1) → multiple dates, so opening+closing stock per month is now feasible. **Re-run the Growyze DV/presentation to confirm `F_INV_COUNTS_DAY` picks up multiple `COUNT_DATE`s** (DL has them; DV must reprocess). ⚠️ **Heathrow still has only 1 stocktake** even over 60 days — a Growyze data-availability limit, not fixable by more backfill; **build/validate on Gloucester**.
- (e) **Turnover window — widened (2026-07-29).** After the 60-day backfill, Gloucester Mews transactional grew to **71 invoices / 72 orders** (was ~49 over 30d / originally 2 days). Richer but still relatively sparse — the month-grain fact now has more than one month of Gloucester turnover to work with.

## Go-live checklist (from final review — do at deploy, not merge)
- Execute `live/DEPLOY.txt` in order; the fetcher gate (step 3) blocks everything needing real data.
- **Re-run `12_group_mapping` before every fact rebuild** if new products/items have loaded (the `ELSE 'Food'` catch-all means Check 2 coverage only catches items loaded after the last grouping run).
- Non-F&B Growyze stock silently inflates the `Food` group via the catch-all — go-live data-quality checklist item alongside the O5 `UOM_COST` gate; numbers provisional until confirmed against the real catalogue.
- **NEVER re-run the mock `../02_margebrut_vis_queries.sql` after go-live** — it shares the VisualisationQueries MERGE key with live `14` and would silently revert the dashboard to mock data.
- Validate `MargeBrutPurchasesBySupplier` (F_PURCHASES_DAY.LINE_TOTAL) reconciles with `MargeBrutPurchasesKPI` (fact PURCHASES) — different measures; rename to "Supplier Spend" if they can't reconcile. Requires Growyze `06`/`07` (F_PURCHASES_DAY) deployed first.

## Progress log
- **2026-06-08** — Mock scripts written, query shapes MCP-verified; not deployed.
- **2026-07-02** — Logged to ledger from memory.
- **2026-07-02 (DB audit, UAT)** — Mock DEPLOYED to UAT (Oak & Vine). `MargeBrut%` = 9 LIVE. Report DB: all 9 mapped; "Marge Brut" OrganisationDashboardConfig present. Caveat: visibility likely blocked by O13.
- **2026-07-10 (mock→live build)** — Scoped/designed/built the live conversion (subagent-driven, 8 scripts + spec + plan, all review-clean; final whole-branch review READY TO MERGE) on branch `worktree-margebrut-live`. Nothing deployed — gated (see above). Branch **kept as-is** per user (2026-07-13) for pickup in a new session.
- **2026-07-22 (dependency split-out)** — Reviewed external dependencies and produced a helicopter-view dependency map. Org plan changed from the single invented "Three Rocks Hotel" org to the **two real Ibis hotels** (GUIDs already in microservice Prod). Split the prerequisites into their own ledger items: **O19** (this project — provision the 2 orgs + map Growyze/Mews across Test/UAT/Prod) and **Integrations O10** (that project — Key Vault secrets per env + initiate loads). `01_provision_uat_org.sql` (single-org, UAT-only) will need reworking to two orgs across three envs when O19 is executed.

- **2026-07-29 (gate (b) satisfied on UAT — Integrations O10)** — Both Ibis hotels now land **Growyze + Mews** on UAT with DV loads completed (30-day backfill, 2026-06-29→2026-07-28). Rough DL volumes: **Heathrow** ≈16.2k rows (Growyze 15.7k / Mews 521), **Gloucester** ≈19.8k rows (Growyze 19.2k / Mews 630). Also cleared en route (Integrations side, shipped v0.1.5 `20260729.1`): the Mews central `api_config` was NULL in UAT (blocked all Mews staging) + two `int_mews001` bugs (products Polars schema inference; CTL_STG_PROCESS PK collision). **Data-readiness for this dashboard:** Consumption (Growyze) ready for both hotels; Turnover (Mews) ready for **Gloucester only** — Heathrow has no Mews POS transactions yet, so build/validate the live dashboard against **Gloucester** first. 30-day window ≈ one month of the month-grain fact; ask for a longer `--load-days` backfill if a multi-month baseline is wanted. Remaining O8 gates unchanged: merge `worktree-margebrut-live`, O5 `UOM_COST`, then `DEPLOY.txt` (and O19 org provisioning is done for UAT/Test; Prod pending).

- **2026-07-29 (branch merged + Turnover unblocked + two new data gates found)** — Picked the item up and audited UAT before acting. Findings and work:
  - **Gate (b) was only half true.** Growyze/Consumption had loaded, but the **Mews/Turnover half had never been deployed to UAT**: `core.int_mews001.StagingControl` = **0 rows**, `EntityMappings` = **0 rows**, no `stage.MEWS_*` tables, `HUB_LINEITEM`/`SAT_LINEITEM` = 0, `F_LINEITEM_15MIN` = 0. Mews DL data had landed but had no path into the DV. (`live/DEPLOY.txt` step 2 / ledger O14 was never run here.)
  - **Fixed it.** Authored `ClaudeDevelopment/integrations/Mews/91_deploy_mews_uat.ps1` (house-pattern runner: env-var connection, `-WhatIf`, `-StartAt` resume, typed confirmation, per-run log, `prod` refusal rail; discovers target orgs from `OrganisationIntegrations` rather than hardcoding). Deployed `01`→`02`→`05`(Section A)→`03` to UAT `core` — verification exactly to spec: **staging=15, load=16, mappings=16, crm=0**. Then ran `sp_DataVaultLoad @SchemaList='int_mews001'` for both Ibis orgs.
  - **Result:** Gloucester `F_LINEITEM_15MIN` **0 → 62 rows** (36 PROD £1,349.02 net / 26 TAX £269.73; gross £1,618.75). DV: HUB_LINEITEM 107, SAT_LINEITEM 107, HUB_CUSTORDER 47, LNK_CUSTORDER_LINEITEM 107, HUB_PRODUCT 389. Heathrow loaded **dimensions only** (HUB_PRODUCT 570, HUB_LOCATION 2, zero transactions) — as expected. **`LINEITEM_TIMESTAMP` is 100% populated on the Mews feed** (the Growyze NULL-timestamp defect does not apply here).
  - **Two new gates found** (see (c)/(d)/(e) above): the O5 `UOM_COST` inflation is **not** resolved — it is live on Ibis at ≈700× via an ml/gr-count vs per-pack-cost mismatch; and there is only **one stocktake date**, so month opening/closing consumption cannot be computed. Mews POS also covers only 2 days.
  - **Merged** `worktree-margebrut-live` into local `main` (fast-forward `aaabc5b..fb52ab3`, 15 commits) without disturbing the in-flight `feature/v1.0-baseline-refresh` working tree. **Not pushed to origin.**

- **2026-07-29 (60-day backfill landed — gates (d)/(e) improved)** — After Release's Growyze DV-load changes, Integrations ran a **60-day** reload for both Ibis orgs (window 2026-05-30→07-28, both completed clean, 9 DV batches each). Result vs the earlier 30-day/2-day picture: **Gloucester** Growyze now has **8 stocktake reports** (was 1 → gate (d) opening/closing now feasible) and Mews turnover **71 invoices / 72 orders** (was 49 → gate (e) widened). **Heathrow** still **1 stocktake** and **no Mews turnover** (data-availability, not a load gap) → **build/validate the live dashboard on Gloucester**. Next Release-side step: re-run the Growyze DV/presentation so `F_INV_COUNTS_DAY` reflects the 8 stocktake dates, then re-check the `UOM_COST` (c) fix against the fuller data.

## Pick-up notes (resume here)
- The work is **merged to local `main`** — but `main` is **not pushed to origin** yet. Push before doing anything that assumes the remote has it.
- Read `docs/superpowers/specs/2026-07-10-marge-brut-live-design.md` + `docs/superpowers/plans/2026-07-10-marge-brut-live.md` for the full design; `memory/marge-brut-dashboard.md` for tokens/VisualisationId map.
- **Turnover is no longer the blocker** — Mews staging/DV is deployed on UAT and Gloucester has real line items. The live blockers are now the two **Growyze data** problems: `UOM_COST` unit mismatch (c) and the single stocktake date (d).
- `live/DEPLOY.txt` steps 1 and 2 are **superseded** — step 1 (`01_provision_uat_org.sql`) is obsolete (O19 already provisioned the 2 Ibis orgs) and step 2 (Mews DV mapping) was completed 2026-07-29. Resume at **step 5** (grouping + fact build), but only once (c)/(d) clear.
- Build/validate against **Ibis Gloucester Road** (OrgID 21, DB `20260722_XMS_67CA4E6F-9A7E-F111-B337-002248A1EC3D`) — Heathrow (OrgID 20) has no turnover.
- Test + Prod still need the Mews control-plane deploy: `ClaudeDevelopment/integrations/Mews/91_deploy_mews_uat.ps1 -Environment TEST` (Prod is human-run only).
- Don't mark Closed until the user confirms.
