# XMS BI Release — Outstanding Work Ledger

**The live status board for all open work on this project.** Kept current as work is done — when an item is started, advanced, or closed, update its row here in the same session.

**Status tags:** `OPEN` = needs work, no fix yet · `DIAGNOSED` = root cause known, fix not actioned · `IN PROGRESS` = fix being actioned now · `MONITOR` = fix applied, watching it · `BLOCKED` = waiting on someone else · `DOC-DEBT` = documentation gap · `CLOSED` = no further work needed (kept on the board briefly with the reason, then removed).

**Last updated:** 2026-08-04 (**O5: PLAN 4 — LAYOUT & FORMATTING — DEPLOYED TO UAT + GATED. All four plans are now live on all 5 Growyze orgs and the 3-dashboard pack has been re-laid-out.** Warehouse 7/7 steps, gate **47 PASS / 0 FAIL**; report DB 2/2 steps, gate **71 PASS / 0 FAIL**; cross-server check PASS over 29 pairs. **32 → 35 cards**, `SortOrder` renumbered to the gapped house convention, heatmap removed, 3 narrative cards added, currency + 12 type tokens fixed. Verified **end-to-end** by executing the deployed templates the way the card SP does — `NetSales` `£224,282`, `InvWasteCost` `£186`, figures **identical** to the design review's observed values so only the symbol changed. **Four defects the review had not found:** all four added cards would have failed **silently** (StaticBoxCard/MarkdownCard ungranted on all 5 orgs, BarChartCard missing on Heathrow — a missing grant makes the dataset-map join find nothing and skip with no error); the review's variance alert **fired at 4,385%** on Padel so it was rejected and re-based off quantities entirely; `InvUseAnalisys` was **actively mislabelling** (OPEN/CLOSE dates and counts swapped, both "variance" columns actually `ACTUAL_USED`, hand-rolled usage disagreeing with the fact's own `ACTUAL_USAGE` in **88%** of rows); and a UTF-8-no-BOM **encoding trap** that would have stored mojibake for every `£`. **Two review instructions were measured and NOT followed** — filtering the `All INVITEMs` row would have hidden **£1,164.56** of counted stock, and dropping `ProductComparison.Subcategory` would degrade the multi-source orgs. New **O40**: three defects diagnosed as SQL work *twice* that are **not** SQL — they are front-end rendering. ⚠️ **v1.1 now has a HARD PROD GATE**: `StaticBoxCard`(16) and `MarkdownCard`(17) are UAT-only and without them the three new cards **error rather than hide**. ⚠️ **O39 is bigger than recorded — THREE branches now carry UAT-live work absent from `main`** (`worktree-growyze-dashboards-o5` +7 with Plans 3 *and* 4, `worktree-margebrut-live` +12, `feature/v1.0-baseline-refresh` +26) and **`origin/main` is 34 behind**; a v1.1 cut from `main` today would omit all of it. Remaining on O5: browser smoke test of the new layout, the **S1–S6** cluster, S7 decision, Task 1c, merges + release-prep. Prior entry: 7/7 write steps OK, gate **PASS=14 FAIL=0**, cross-server check PASS over 30 pairs, and every insert count exactly as predicted. Re-verified independently through MCP; **Ibis Heathrow provisioned from nothing** with Gloucester proven untouched. Rev 2 first corrected five stale facts in rev 1 and its audit raised **O37** (`F_INV_DAILY_DETAIL.UOM_COST` NULL everywhere ⇒ a *live* card blank on all 5 orgs, dropped from the pack) and **O38** (95% of Oak & Vine's count rows have no `D_INVITEM` row ⇒ a grid silently showing 5%). Remaining on O5: the browser smoke test, Task 1c, release-prep. **Reconciled onto other items in the same pass:** **O34's DB half is DONE** — Padel's `DbPrefix` was corrected by Plan 3 step 1, so all 20 `BiConfig` rows now resolve to a real database; only its doc fix remains, so it moves to DOC-DEBT. **O19 had a gap in its definition of "complete"** — it provisioned the MI but not the `report` DB, and the two Ibis orgs had silently diverged; carry "an org is not provisioned until it exists in BOTH systems" into its Prod half. **O24** explains the Oak & Vine venue-label collision that O5 Task 1c will fix. New: **O39** — O23's fix is live on UAT but exists only on the unmerged `worktree-margebrut-live` branch, so `main` lacks it and a v1.1 cut from `main` would silently omit it)

---

## At a glance

> **Each ID links to a detail file** under [`outstanding/`](outstanding/) holding that task's full context, **progress log**, and pick-up notes. Read the one file for the task you're picking up — detail files are deliberately *not* loaded at session start, so this board stays cheap to keep in context. This table is the only thing surfaced at session start.

| ID | Pri | Item | Area | Status | Owner / decides | Next action |
| --- | --- | --- | --- | --- | --- | --- |
| [O1](outstanding/O1-xmse944-marketman-load-failure.md) | 3 | XMSE-944: MarketMan dup-INVITEM_ID PK bug | MarketMan / DV load | MONITOR | Andy | Audit: 0 dupes — bug gone. Confirm & close; DEV pipeline dormant since ~Mar 10 |
| [O2](outstanding/O2-inventory-variance-1315.md) | 3 | Inventory-variance fix: scripts 13-15 not deployed | Inventory / presentation | OPEN | Andy | Not DB-verifiable — confirm vs QUERY_STATUS/deploy log |
| [O3](outstanding/O3-suggestion-redesign-xmse948.md) | 3 | Suggestion system redesign (XMSE-948): 01-07 not deployed | Suggestion / AI engine | OPEN | Andy | Audit confirms NOT deployed (both envs). Deploy 01-07 |
| [O4](outstanding/O4-parent-org-1011.md) | 3 | Parent-org reporting: scripts 10-11 | Parent-org / reporting | MONITOR | Andy | Audit: UAT holds 21 Parent queries + tiers 100-102 — likely already deployed. Confirm 10-11 & close |
| [O5](outstanding/O5-growyze-default-dashboards.md) | 1 | Growyze default dashboards rebuild | Growyze / presentation | IN PROGRESS | Andy | 🆕 **2026-08-04 — PLAN 4 (LAYOUT & FORMATTING) DEPLOYED TO UAT + GATED. All four plans are now live on all five orgs.** Warehouse 7/7 / **47 PASS 0 FAIL**; report DB 2/2 / **71 PASS 0 FAIL**; cross-server PASS over 29 pairs. **32 → 35 cards**, gapped `SortOrder`, heatmap removed, 3 narrative cards added, currency + 12 type tokens. Row arithmetic reconstructed from live data: **19/19 emergent rows total exactly 12**. End-to-end: `NetSales` `£224,282`, `InvWasteCost` `£186` — identical to the review's figures, so only the symbol changed. **Caught before shipping:** all four added cards would have failed **silently** (grants absent on all 5 orgs / Heathrow); the review's variance alert **fired at 4,385%** and was rejected, then re-based onto dates + cost-nullity only (a variance % is a ratio of two *different* measures so the scale defect propagates; the retained waste trigger compares the *same* measure over time, where it cancels); `InvUseAnalisys` was mislabelling 5 ways; and a **UTF-8-no-BOM** trap would have stored mojibake for every `£`. **Two review instructions measured and NOT followed** (filtering `All INVITEMs` would hide **£1,164.56**; dropping `Subcategory` would degrade the multi-source orgs). **S9 fixed; S10 → O40; S12 moot; S7 root-caused and awaiting a decision; S8's recorded remedy is now known to be unsupported — do NOT apply the `LEFT JOIN`+`Unmapped` fix.** **Next: browser smoke test (heatmap actually gone? `xl=2` at ≥1536px?) → S1–S6 → S7 decision → Task 1c → merges + release-prep.** ⚠️ **Prod gate: `StaticBoxCard`+`MarkdownCard` must reach Prod before this layout does, or the new cards error rather than hide.** ⚠️ **This branch is +7 on `main` and NOT merged** — the pick-up note that used to say "the branch is merged" is stale; see O39. ── Prior state: ✅ **Plan 1 + precedence + PLAN 2 all DEPLOYED & VERIFIED on UAT, all 5 orgs (2026-07-30/31).** Plan 2: 12/12 steps OK, **15 datasets LIVE**, all 9 control-plane checks PASS on every org, `InvMargeBrut` XMSE-1099 fixed, and the **regression witness is unchanged pre→post** (Padel £148,897.95/7,600, Oak & Vine £905,503.87/64,724, Dirty Sixth £317,414.99/12,906). Deployed templates render correctly end-to-end: Active Stocktakes `2 / 3` on Padel, Best Category `Beverages · 82.0%`, and **the no-cost state works — Heathrow `—` (no sales) vs Gloucester `No cost data`** (703 rows, 0 costed, O35). Rev 2 first prevented five *measured* defect classes rev 1 would have reintroduced (no resolver ⇒ 6 cards empty on 3 orgs; `MIDDLE_1` grain = 98 Mews product families; `ParameterMappings` unset ⇒ dead date picker; `BOTTOM_LEVEL_NAME` ⇒ 1-of-3 venue denominator; coverage-mismatched ratios), and 3 more defects were found in the plan's own SQL by executing it. **PLAN 3 IS DEPLOYED TO UAT AND VERIFIED (2026-07-31) — so all three plans are live and the pack is wired and navigable on all five orgs.** `90_deploy_plan3.ps1`: **7/7 write steps OK**, gate **PASS=14 FAIL=0 WARN=1 VACUOUS=0**, cross-server check **PASS over 30 pairs / 0 mismatches** (log `deploy_PLAN3_UAT_20260731_134240.log`). Every insert count matched prediction exactly: 15 grants, 117 dataset-map rows (13 already present), 3 grids, 15 configs, 15 group mappings, 8+2 / 15+3 / 9+2 items+filters. **The pre-deploy baseline is what makes that PASS mean something** — it read PASS=6 **FAIL=6**, so this is a before/after diff, not equality against a recorded figure. **Re-verified independently through MCP** on a different connection, because the A-checks largely *mirror the runner's own inserts*: `SortOrder` landed per org exactly as computed (Padel **14/24/34**, Oak & Vine **110/120/130** — clear of `Weekly P&L` at 10 — others **10/20/30**); Heathrow holds 1 `BiConfig` + 1 group while **Gloucester still holds exactly 1 of each** (the GUID-mix-up counter-check); Padel now reads `20260310` (**O34** fixed); `BiConfig` 19→20 rows for 20 orgs. **A7 stayed WARN at 2 collisions but across 38 dashboards rather than 23 — the pack added 15 and introduced ZERO new ones** (both pre-date it: Dirty Sixth's 5 and Gloucester's 2 all sit at 0), which is the evidence the per-org computation worked rather than merely not erroring. **Checked because Heathrow had never rendered a dashboard:** all **9** pack card SPs exist in `core` on all five org DBs, so its whole path is complete. ⚠️ **This does NOT prove the dashboards render** — no browser has been opened; that is Task 7 Step 6 and the one gate left. Plan 2 already verified all 15 templates return correct data on all five orgs and Plan 3 changed no template, so the residual risk is presentation-layer, not query-layer. **Next: browser smoke test → Task 1c (venue label, MI, script `24`) → release-prep `v1.1`.** Rev 1 was written for 2 orgs against a report DB that has since moved; re-measuring found **five** things wrong with it — **Ibis Heathrow has NO report-DB presence at all** (no `BiConfig`, no grant, no group, no dashboard — new Task 1b provisions it; its GUID is `7CE02464-…`, not Gloucester's `67CA4E6F-…`, and both share prefix `20260722`); `SortOrder` 10/11/12 would have **collided** with Oak & Vine's `Weekly P&L` (that org has **11** dashboards, not 4); **filters need no card-type grant and no dataset-map row** (proved by Gloucester rendering 3 filters with neither — rev 1's row would have silently no-op'd on 3 of 5 orgs); per-org grant gaps are all different; and the report DB is on `xms-sql-fog-uat`. **The shared-dataset audit the pick-up notes demanded is DONE** — 16 LIVE rows, and **0 of 16 carry ANY source scoping**; measured, that only bites on **Oak & Vine** (sales NCRAloha £2.73M + Mews £55k with Growyze at **£0**; inventory Growyze 465 vs MarketMan 468) and is a provable no-op on the other four, which carry one source per fact. `BOTTOM_LEVEL_NAME` offenders: **0** — a first-pass flag on `InvUseAnalisys` was **withdrawn** on reading the SQL (select-list column, not a filter). Executing the audit found **two defects no rule would catch: O37** (a card blank on all 5 orgs, on **two** of the three dashboards — dropped) and **O38** (a grid silently showing 5% of Oak & Vine's data). Pre-deploy baseline **PASS=6 FAIL=6 WARN=1 VACUOUS=2**, every verdict expected, and it independently cross-checks the audit (117 of 130 pairs unwired = exactly the 13 already present; 15 missing grants = exactly the matrix); **A4/A6 report `VACUOUS`**, not a false PASS. One verifier defect found by running it (Msg 130, aggregate over a subquery). **All 4 gating decisions settled:** provision Heathrow · drop `OakVineInvTotalCost`, keep `InvWasteCost` · accept Oak & Vine's mix (names are source-neutral) · wire the Ibis cost cards with `No cost data` + fix the venue label via `MICROSERVICE_NAME`. ⚠️ **O23's deployed fix lives only on the unmerged `worktree-margebrut-live` branch** — live on UAT, absent from `main`; merge before cutting v1.1 (this is also why `22`/`23`/`94` are taken and Task 1c uses `24`). Sub-items: **O33** (Pri 1) / **O35** / **O36** / **O37** / **O38** |
| [O6](outstanding/O6-growyze-reporting-staging-fixes.md) | 3 | Growyze reporting + staging fixes | Growyze / staging | MONITOR | Andy | Audit: all 3 fixes observed live on UAT. Confirm & close |
| [O7](outstanding/O7-xmse949-product-cost-matching.md) | 3 | XMSE-949: Product cost matching refactor | Presentation / margin | DIAGNOSED | Andy | Audit: cost-map is weak (95% NULL) AND fans out ~1.5× (= O10). Build PRODUCT_COST_MAP |
| [O8](outstanding/O8-marge-brut-dashboard.md) | 1 | Marge Brut dashboard — DEPLOYED + VERIFIED on 2 UAT orgs; 8 cards, date + group filterable | Dashboard / vis + Report DB | MONITOR | Andy | **2026-07-30 (F&B group filter added — `live/19`+`20`, deployed + verified both orgs).** Asked what filters beyond date make sense; grounded in the grain the answer is **exactly one**: `F_MARGEBRUT_MONTH` is `GROUP_NAME × PERIOD_MONTH`, so `GROUP_NAME` is the only remaining filterable axis (outlet / revenue centre / supplier / property each need a new fact column; revenue centre and supplier are also *wrong* dashboard-wide, since turnover carries `REVCENTER_HUB_ID` and stock doesn't). Built as a **bespoke `MargeBrutGroups` widget sourced from the fact's own `GROUP_NAME`** rather than reusing `ProductCategories` — that lists `D_PRODUCT` names (`'Wine'`, not the fact's `'Wines'`) and isn't source-scoped, so a pick would have emitted `GROUP_NAME IN ('Wine')` and matched nothing: the same silent-discard class as the date bug. **Contract verified, not assumed:** `DashboardGridFilter.DataSet` == the per-card `FilterDefinitions` key (each bespoke `RedLion*` widget appears on both sides) — which retro-explains the dead `PeriodMonth` key: a key with no widget is never emitted, so a *new* key is legitimate and only the widget row was missing. ⚠️ **New reusable platform trap: `BuildDynamicWhereClause` holds every mapped column in an `NVARCHAR(100)` and assigns `JSON_VALUE` straight into it, so a longer expression is SILENTLY TRUNCATED** (applies to `FilterDefinitions` columns and all three `ParameterMappings` slots). The ~700-char group `CASE` was therefore unusable as the supplier card's filter column ⇒ `19` restructured that query's `D_INVITEM` join into a derived table exposing `inv.GROUP_NAME`, keeping aliases `p`/`inv` so `18`'s date mappings still resolve; **measure-neutral, £22,544.37 unchanged**, and check 4c now asserts the ≤100 rule so it can't regress silently. The group `CASE` now lives in **three** places (`13` + twice in `19`) — keep in step. **Verified by EXECing the real card SPs on both orgs** (June, Wines only): Oak & Vine turnover £17,138.71→£381.20, cost 27.3%→**59.5%**; Gloucester 27.4%→**59.5%**; supplier £2,652.84→£70.01 with bars *and* header responding; grid GRAND TOTAL 59.5% **agreeing with the hero KPI and the by-group bar** — which independently corroborates the open "Wines 59.5% looks high" question. **Expected, not a defect:** with a group selected the grid keeps all 9 rows, unselected ones blank — its `canon` VALUES list can't be filtered because the engine builds *one* clause and injects it at every site, so a site over `canon` would also get the date predicate on a CTE with no `PERIOD_MONTH`; chosen over leaving the grid unfiltered, which would make GRAND TOTAL disagree with the hero KPI. **Next: UI re-check as Oak & Vine, exercising both filters.** Prior — **2026-07-30 (first UI review): 3 card defects + a dead date filter, all 4 fixed and deployed same day. Dashboard is now 8 cards and date-filterable for the first time.** (a) **Cost of Sales % by Group showed a `0.00` headline** — `14` returned `NULL AS TotalValue` believing a % grand-total wasn't meaningful, but **BarChartCard parses `TotalValue` as numeric and renders NULL as `0.00`**, so the card asserted a wrong number rather than omitting one; now returns the hero KPI's coverage-matched ratio → **14.8**, agreeing with the KPI card and grid GRAND TOTAL (`live/16`). (b) **Food Purchases by Supplier empty** → was [O23](outstanding/O23-growyze-supplier-bottom-level.md), fixed; now 3 bars = £22,544.37. (c) **Comps & Staff Meals withdrawn** (`live/17`) — unpopulatable on *any* org: `COMP` is a hardcoded literal `0` awaiting a comp-specific Mews signal, and `STAFF_MEAL` reads `reference.MARGEBRUT_MANUAL` which only ever got `11`'s **Oct-2025** seed vs Apr–Jul 2026 data (Gloucester holds only `2025-10`; Oak & Vine's table is **empty**). A data gap, not a query defect ⇒ soft-deleted the grid item (9→8 cards both orgs, PASS, reversible in one statement; verified `DashboardGrid_Load` really filters `IsDeleted=0` first). (d) **Date filter did nothing** (`live/18`) — it was wired to the wrong one of the platform's two filter channels: `BuildDynamicWhereClause` reads the date columns from **`ParameterMappings`**, and all 9 datasets shipped `ParameterMappings = '{}'`, so `@StartDate`/`@EndDate` were **silently discarded**; `14` had invented a `FilterDefinitions` key `PeriodMonth` that exists on these 9 datasets and **nowhere else in the platform** (no date-typed filter key exists at all). Mapped to a **month-overlap** test — the naive bare-column mapping drops the opening month of any mid-month range (15 Jun→15 Jul: **June gone**, cost NULL) yet passes on whole months, so it would have shipped broken; supplier card mapped to month-derived time-free expressions because `ORDER_DATE` is `datetime2` (raw `<=` lost 16 lines / **£129.45**). **Verified by EXECing the real card SPs:** turnover £55,221.02 → **£17,138.71**, cost 14.8% → **27.3%**, supplier £22,544.37 → **£2,652.84**. **This also dissolved the apparent ~2.5× Purchases gap** — £13,326.32 of PO lines sit in months with no fact row, so unfiltered the supplier card summed ~17 months vs the grid's 4; **June reconciles to £0.00**, confirming the board's earlier "reconcile exactly on an equal window" claim was right and the Task-7 TODO stays closed. **New presentational question:** a month with no stocktake coverage now yields a *blank* cost/GP card (Gloucester + May 2026) — decide alongside the >100% GP question. **Next: UI re-check as Oak & Vine.** Prior — **2026-07-30: also live on The Oak & Vine (org 16)** after the 90-day Mews+Growyze load — `99_verify` **5-of-6 PASS on both orgs**, June cost **27.3%** there vs **27.4%** on Gloucester. Two fixes needed first: (a) Oak & Vine's dashboard was **erroring**, not empty — `14` is global and had repointed its 9 mock cards at a fact table that only existed on Gloucester (**lesson: a global vis-query swap breaks every org wired for the dashboard but lacking the fact table**); (b) **source collision** — Oak & Vine carries NCRAloha + MarketMan too, and NCRAloha's `TOP_NAME` is also `'Food'` worth **£916,792.80** vs the Mews Food group's £281.62, so cost of sales would have read ~**0.5%**. Fixed by pinning each side to its feed via `BOTTOM_SRC` (`int_mews%` turnover / `int_growyze%` stock), which **retires the design's "dedicated Mews+Growyze-only org" constraint**. `99_verify` filters updated to match. ⚠️ Oak & Vine is a **mirror** of Gloucester's feed (June turnover identical to the penny) and its **May is untrustworthy** (first-count anomaly → Soft Drinks GP 105.2%) — quote June only. **Next: UI check as Oak & Vine** (Ibis orgs await Ian's front-end config). Prior 2026-07-29: **all of `live/DEPLOY.txt` done on Ibis Gloucester Road (OrgID 21).** Fact built (18 rows; **June 2026: turnover ex-VAT £17,138.71, consumption £4,701.37, cost 27.4% / GP 72.6%**), 9 vis queries live, **report DB wired** (1 dashboard, 9 cards, group-mapped), **`99_verify` 5-of-6 PASS**: fact self-consistency 27.4312% both sides, grain sanity equals an independent recompute to the penny, coverage **£0.04 of £33,144.54** turnover and **£0.00 of £18,302.55** stock excluded, purchases reconcile **exactly** on an equal window (£2,652.84, delta £0.00 — Task-7 TODO **closed**). **4 defects fixed en route:** `12_group_mapping` withdrawn (wrote group strings into `MICROSERVICE_NAME`, the display-name resolver behind 82 COALESCE sites — grouping now inline off `TOP_NAME`); `OPENING`/`CLOSING` were the **same** month-end stocktake so consumption collapsed to purchases; `PURCHASES` ~3× low; coverage-mismatched cost-% denominators (read 14.3% vs the real 27.4%). `99_verify` itself was stale and rewritten — it had been giving **false FAILs** and validating a formula the dashboard no longer used; also fixed a `datetime2` end-date boundary bug in it. **Remaining: UI check only.** 9th card empty → new **[O23](outstanding/O23-growyze-supplier-bottom-level.md)** |
| [O9](outstanding/O9-demo-org-not-provisioned.md) | 3 | Demo org data generator: org not provisioned | Demo data / provisioning | OPEN | Andy | Audit confirms not provisioned (both envs). Provision via SP, load data |
| [O10](outstanding/O10-threerocks-sales-double-count.md) | 3 | £580k Three Rocks sales over-count | Sales facts / correctness | DIAGNOSED | Andy | Audit: confirmed ~1.5× join fan-out in F_PRODUCT_MARGIN_DAY cost map (= O7). Dedup cost map |
| [O11](outstanding/O11-square-integration-scripts.md) | 3 | Square integration: SQL scripts not built | Integrations / Square POS | OPEN | Andy | Build scripts from approved design + plan (not DB-audited) |
| [O12](outstanding/O12-doc-debt.md) | 3 | Doc debt: DV diagram stat + audit findings §8-10 | Documentation | DOC-DEBT | Andy | Fix Live Links stat 39→40; address §8-10 |
| [O13](outstanding/O13-uat-dashboard-groupmapping-empty.md) | 3 | ~~UAT DashboardGroupMapping empty → dashboards invisible~~ | Report DB / visibility | **CLOSED** | Andy | **Misdiagnosis — no work needed.** O13 audited `DashboardGroupMapping` (unused platform tier); the load-bearing junction is `OrganisationDashboardGroupMapping` = 86 live rows since 2026-05-18, 0 ungrouped configs. O4/O8 never blocked here |
| [O21](outstanding/O21-report-db-out-of-band-procedures.md) | 2 | Report DB: 12 procedures hand-deployed 2026-07-06, absent from RoundhousE | Report DB / release process | DIAGNOSED | Andy | Commit the `_Delete` family + 2 `_Create` upserts as numbered migration scripts before Prod dashboard config — Prod currently has only hard-delete `_DeleteEntity` |
| [O22](outstanding/O22-report-db-audit-trigger-regression.md) | 2 | Report DB: 2 audit triggers block the platform-default and staff config tiers | Report DB / correctness | DIAGNOSED | Andy | Fix `DashboardConfig_Audit` + `StaffDashboardConfig_Audit` to populate `DashboardGridId`/`IconName`/`SortOrder`; both tiers unwritable until then |
| [O25](outstanding/O25-d-discount-microservice-id-typing.md) | 2 | `D_DISCOUNT.*_MICROSERVICE_ID` typed `uniqueidentifier` → build dies on non-GUID text | Presentation / table DDL | DIAGNOSED | Andy | Raised out of [O24](outstanding/O24-oak-vine-growyze-mews-mapping.md)'s log, where a data workaround (clearing Oak & Vine's NCR dummy discounts) hid a defect that **remains**. **O24's recorded diagnosis was wrong** — there is no `uniqueidentifier` cast in the `Discount Dimension` SQL, and `SAT_DISCOUNT.DISCOUNT_ID`/`MICROSERVICE_ID` are both `nvarchar`; the cast is in the **target table DDL** and fires on `sp_ExecuteQuery`'s final INSERT (Msg 8169). **`D_DISCOUNT` is the only 1 of 15 dimensions** typing these `uniqueidentifier` (the other 14 use `nvarchar`) — confirmed in the live table and in the registered `ddl_script` + `column_definitions`. Fix: change those 3 columns to `[nvarchar](100)`, redeploy **that table only** per affected org (never `DeployPresentationTables`), rebuild. Will recur for **any** org with NCR Aloha discount data. Also check whether NCR Aloha staging is populating `MICROSERVICE_ID` at all — it's a manual-only MDM column |
| [O26](outstanding/O26-org-palette-never-applied.md) | 3 | Org palettes show as an option but cannot be a default; selection doesn't persist | Report DB / palettes + front end | **BLOCKED — with FE as [XMSE-1756](https://threerocks.atlassian.net/browse/XMSE-1756)** | Ian Hamlin (FE) | **Two causes, one already fixed.** (1) *Not appearing* = **stale cache** — Ian cleared it 2026-07-30 and **Growyze now shows in the picker** under an `Organisation` heading, so the FE does consume all 3 definition tiers correctly. (2) *Not defaulting* = **never built** (Ian: *"I dont think we added a default? Just the ability to add palettes"*). **Key finding from the live UI:** the picker's `Default → Rainbow` **does not exist in the database** — card colours today come from a hardcoded client-side default, so the 90-colour store is unused until someone picks. `Core` is a byte-exact match for the 11 `DashboardPalette` rows. Selecting Growyze **previews live** (cards render `#000055`/`#34DBD1`/`#FC3762`/`#F3F3FF`) but **all 3 selection tables stay at 0 live + 0 audit rows** — so nothing persists. **This makes the fix much smaller than first thought:** the client already renders arbitrary palettes and already knows which is the org's, so option A is just *"start from the org palette instead of `Rainbow`"*, not new theming. DB persistence layer already exists unused (RoundhousE `060`–`068`). ✅ **Raised as [XMSE-1756](https://threerocks.atlassian.net/browse/XMSE-1756)** (Story, Ian Hamlin, backlog — agreed not to be worked on now). Nothing outstanding Release-side. ⚠️ **Reusable gotcha from this:** the app **caches report-DB config and a direct SQL write does not invalidate it** — the admin tools do. These palettes were correct in the DB from 2026-04-16 but invisible for 3 months. **Query the tables before concluding a hand-run report-DB change failed.** Caveats in the ticket: no org-scoped selection table exists (all 3 need `StaffId`) and `PaletteId` is an FK-less pointer across 3 parents |
| [O23](outstanding/O23-growyze-supplier-bottom-level.md) | 3 | Growyze suppliers never set `BOTTOM_LEVEL` → `D_SUPPLIER` cannot build | Growyze / staging + presentation | MONITOR | Andy | **FIXED + DEPLOYED to UAT 2026-07-30 (orgs 16 + 21), verified PASS on both** — `bl_set 8/8`, `D_SUPPLIER` 8 Growyze rows, **0 of 2,438** purchase lines unmatched (was 2,438). The 9th [O8](outstanding/O8-marge-brut-dashboard.md) card now renders 3 bars: Bidfood £16,535.89 / Reynolds Catering £3,782.45 / Matthew Clark £2,226.03 = **£22,544.37**, reconciling exactly with the header total that had been resolving all along. Scripts `Growyze/22`+`23`, runner `94` (branch `worktree-margebrut-live`). Fix mirrors the flat `Growyze Location` step (`'Supplier' AS LEVEL_NAME, 1 AS BOTTOM_LEVEL, NULL AS PARENT_ID`) **plus** the 3 columns on the `SUPPLIER` `EntityMappings` row so `UploadEntityMappings` regenerates the Load step. ⚠️ **`MICROSERVICE_NAME` deliberately NOT carried** — staging hardcodes the literal `'growyze'`, and the card labels bars with `COALESCE(BOTTOM_MICROSERVICE_NAME, BOTTOM_SUPPLIER_NAME)`, so carrying it would collapse 3 suppliers into **one bar named "growyze"**: an empty chart traded for a plausible wrong one. `23`'s check 2 asserts it stays NULL. **Next: confirm in the UI, then reload the 3 un-reloaded Growyze orgs (10 Padel Social, 18 Dirty Sixth, 20 Ibis Heathrow) — the control-plane change is shared, so they apply it unattended on their next scheduled load — and close.** Root cause (proven): the `Supplier Dimension` step anchors on `WHERE BOTTOM_LEVEL = 1` but `Growyze Suppliers` staging never populated it (its 4 sibling dimension steps all do); MarketMan *does* set it, so the defect was Growyze-specific |
| [O14](outstanding/O14-mews-dv-mapping-deploy.md) | 3 | Mews001 DV mapping — **DEPLOYED to UAT**; Test + Prod remain | Integrations / Mews POS | MONITOR | Andy | ✅ **UAT DONE 2026-07-29** as a side-effect of [O8](outstanding/O8-marge-brut-dashboard.md) — the control plane had never been deployed there (`StagingControl`/`EntityMappings` both **0**), which is why Mews turnover was dead. Deployed `01`→`02`→`05`§A→`03` via the new `integrations/Mews/91_deploy_mews_uat.ps1` runner; **re-verified 2026-07-30: staging=15, load=16, mappings=16** — exactly to spec. DV loads then ran for both Ibis orgs and for Oak & Vine (O24), producing real `F_LINEITEM_15MIN` rows and a working Marge Brut dashboard, so the mapping is proven end-to-end rather than just present. **Remaining: `91_deploy_mews_uat.ps1 -Environment TEST`** (SQL DB was paused), then **Prod** (human-run per the safety model). Note the old next-action referenced DEV proxy org 19 — superseded |
| [O15](outstanding/O15-prod-v1-launch-followthrough.md) | 3 | v1.0 Prod launch: deployed + validated 2026-07-06, follow-through remaining | Release / Prod launch | OPEN | Andy | Merge PR #2 → promote baseline to root + tag v1.0-baseline → real orgs, report-DB config, fetcher pointing |
| [O16](outstanding/O16-uat-hygiene-prod-deploy-defects.md) | 3 | UAT hygiene: 3 defects surfaced by Prod deploy (widths/SP, extractor idempotency, RuleOverrides record) | UAT / deployment tooling | DIAGNOSED | Andy | Run 96 on UAT + patch UAT's sp_CreateIntegrationTables (regen-regression risk), then the 2 smaller fixes |
| [O17](outstanding/O17-credential-rotation.md) | 3 | Rotate exposed ChickenShop credentials (storage key + Yooz password) | Security / integrations | BLOCKED | Andy | Regenerate chiknstorageaccount key + reset Yooz support@sixsevens password (Azure/Yooz admin, outside repo) |
| [O18](outstanding/O18-xms-report-dataset-sql-exceptions.md) | 3 | UAT xms-report dataset SQL/mapping exceptions (XMSE-1496 & XMSE-1498) | Reporting / dataset SQL | OPEN | Andy | Fix Marge Brut / inventory report-pack dataset SQL (FormatException + aggregate-in-WHERE); both tickets trace to it |
| [O19](outstanding/O19-ibis-org-provisioning-mapping.md) | 2 | Provision 2 Ibis hotel orgs + map to Growyze & Mews (Test/UAT/Prod) | Provisioning / org + integration mapping | IN PROGRESS | Andy | **Scripts staged; Andy running in the morning** (`ClaudeDevelopment/ibis-provisioning/` 01–04 + DEPLOY). **O10 handoff mapping deterministic** (fixed prefix `20260722` + reused GUID ⇒ env-independent `kv_prefix`; GUIDs the only fill-in). **UAT + TEST COMPLETE 2026-07-24** — both Ibis orgs live + mapped in each (UAT OrgIDs 20/21, Test OrgIDs 7/8; all ACTIVE; int_growyze001 13 DL / int_mews001 21 DL). O10 handoff `kv_prefix` values concrete + env-identical (see O19 detail) — **Integrations O10 unblocked**. All via Invoke-Sqlcmd (MCP `execute` 15s cap can't do org creation). **Remaining: Prod only** (human-run via runner per safety model); give O10 the mapping + Growyze org GUIDs. **Prereq for O8 — now DISCHARGED for UAT** (O8 is deployed and verified on Ibis Gloucester Road off these orgs, 2026-07-29); O19's Prod half remains a prereq only for taking Marge Brut to Prod. **Integrations O10 unblocked** and has since completed its loads. ⚠️ **A gap in this item's definition of "complete", found 2026-07-31 by [O5](outstanding/O5-growyze-default-dashboards.md):** provisioning covered the **MI** (org row, database, integration mapping, DL tables) but **not the microservice `report` DB**, and the two Ibis orgs diverged silently — Gloucester acquired report-DB config incidentally via O8/O32, while **Ibis Heathrow had none at all** (no `BiConfig` row, no card-type grant, no `DashboardGroup`, no dashboard) and was the only one of 20 UAT orgs missing a `BiConfig` row. O5's Plan 3 created Heathrow's `BiConfig` + root `All Dashboards` group and granted its 8 card types on 2026-07-31, so UAT is now whole. **Carry this into the Prod half: an org is not "provisioned" until it exists in BOTH systems** — the MI half alone leaves it invisible to the dashboard UI. The report-DB provisioning order (BiConfig → VisualisationConfig → VisualisationDataSetMap → DashboardGrid/Item/Filter → OrganisationDashboardConfig → DashboardGroup → OrganisationDashboardGroupMapping — the last two being the most-missed) is in `docs/microservice-report-database.md` |
| [O20](outstanding/O20-presentation-microservice-name-to-id.md) | 3 | Presentation: switch cross-integration joins from MICROSERVICE_NAME to MICROSERVICE_ID | Presentation / dimension builds + vis queries | OPEN | Andy | **No longer blocked on the mechanism existing** — Integrations O8's MDM registry is **deployed and validated on Dev** (2026-07-30: all 20 org DBs, `06_validate.sql` all-PASS, T1-wipe self-heal proven on real data). Blocked now only on **real ID coverage** — Dev's identities are synthetic-smoke-tested-then-cleaned-up, not a genuine microservice-fed population, and O8 hasn't reached Test/UAT/Prod. Today alignment still resolves on **name** (133 refs in `8_PresentationControl.sql`; `MICROSERVICE_ID_BIN` used **0** times) — a workaround, because GUID text was never canonicalised (CI-collation text compares hide casing while `SHA256Hash` is byte-sensitive). ID columns are **already plumbed** through all 3 dimension tiers into `D_*`, so O8 lights them up with no presentation change; this item moves only the *joins*. Then: inventory genuine name-joins vs pass-through, pick `MICROSERVICE_ID` vs `_ID_BIN`, migrate per entity (LOCATION first) behind row-count + label diffs, decide NULL-coverage fallback. **Gates** enabling O8's `MICROSERVICE_NAME` push-down rule (until then a canonical rename would change join identity, not just labels) |
| [O24](outstanding/O24-oak-vine-growyze-mews-mapping.md) | 2 | Map The Oak & Vine (org 16) to Growyze + Mews in UAT (mirror Ibis Gloucester Road feed) | Provisioning / org + integration mapping | MONITOR | Andy | ✅ **END-TO-END PROVEN 2026-07-30 — nothing outstanding on this item, awaiting Andy's confirm to close.** [O8](outstanding/O8-marge-brut-dashboard.md) has now consumed this feed successfully on org 16: `F_MARGEBRUT_MONTH` built from it (24 rows, June cost 27.3%) with `99_verify` 5-of-6 PASS, so mapping → DL → DV → presentation → dashboard is demonstrated on the mirrored org. ✅ **Release side DONE + VERIFIED on UAT 2026-07-29** via `ClaudeDevelopment/oak-vine-mapping/` runner. Both mappings created (OrgIntegrationID 35 Growyze / 36 Mews, `IsEnabled=1`, `SyncStatus=PENDING`); trigger provisioned **13 Growyze + 21 Mews** DL tables (no `DL_CUSTOMERS`) — both PASS, and the DL table names are **byte-identical to Ibis Gloucester Road** (0 missing / 0 extra), so the mirror is proven. `kv_prefix` values unchanged from those predicted, so **O10's runbook needs no edit**. ✅ **Integrations O10 has now completed the load** (per-feed `reload_integration`, 90-day): Growyze DL 19,233 (=Gloucester), Mews `SAT_CUSTORDER` 1,613 (1.53×≈90/60 of Gloucester), 26 DV jobs / 0 errors. O10 had to clear org 16's NCR Aloha **dummy** discount data (its `D_DISCOUNT` build casts non-GUID `DISC001…` → `uniqueidentifier`, dooming DV) — **warehouse cast fragility remains** (O20/presentation fix candidate). Don't close until Andy confirms. **UAT only** — Test/Prod need a follow-on. **One consequence surfaced 2026-07-31 by [O5](outstanding/O5-growyze-default-dashboards.md):** because orgs 16/20/21 share a single Growyze tenant, Oak & Vine's one Growyze location is *named* **"Ibis Gloucester Rd"**, so `GrowyzeHighestVenue` renders that label on org 16. The money is right, the label is not. O5's **Task 1c** fixes it by hand via `MICROSERVICE_NAME` (the display-name resolver, sanctioned for manual entry) — **the venue's real name still needs agreeing with Andy.** Nothing to do on O24 itself; noted here because the mirrored feed is why the label collides |
| [O27](outstanding/O27-makers-of-hospitality-discovery.md) | 2 | Maker's of Hospitality: discovery follow-through + source-system assessment (0/8) | Pre-sales / prospect discovery | OPEN | Andy (technical) · Kerri + Scott (commercial) | **Hard external deadline — Kerri committed to a next-steps response w/c 2026-08-03**, then a follow-up meeting booked. Two-site independent gastro-pub group, ex-Peach Pubs owners, **guest crossover between sites confirmed** (*"we do cross-pollinate our guest base"*). The thing being bought is the removal of a **~2-hour Monday-morning manual assembly job** across 6 systems + 2 spreadsheets, and a move from *monthly* management accounts to a *weekly* snapshot. **8 blind spots named unprompted** — supplier price movement (*"a complete blind spot"*), stale/ambiguous recipe costs, food GP missed by ~£1,000/wk, unknown gift-voucher liability, untrustworthy spend-per-head, no date filter in Access (week granularity only), constant per-site switching, manual PLU stock-out prevention. **Detail file carries 17 timestamped screenshot points** across the 3:36–34:00 screen share (top 4: recipe costing + duplicate milk products **21:36**, the manual invoice spreadsheet **11:43**, the Access weekly business sheet **5:00**, the browser tab bar **3:42**). **Do sub-task 7 before sizing anything** — a surprising amount is already built ([O8](outstanding/O8-marge-brut-dashboard.md) Marge Brut ≈ the weekly food-GP view; [O4](outstanding/O4-parent-org-1011.md) ≈ two-site rollup with per-GM access; [O7](outstanding/O7-xmse949-product-cost-matching.md)/[O10](outstanding/O10-threerocks-sales-double-count.md) = the same duplicate-product/stale-cost defect class). ⚠️ Product identities need confirming before any vendor contact ("Actio" → almost certainly **Acteol**, itself an Access Group product, so possibly one commercial conversation; "Collins"/"Toggle" unconfirmed). ⚠️ **Not a commitment:** Scott's footfall-tracking contact was explicitly *"me thinking out loud"* — keep it out of the proposal, and note **Wireless Social may already answer it** from data the prospect already pays for. Next: assess sources ([O28](outstanding/O28-access-hospitality-epos-integration.md) + [O29](outstanding/O29-makers-secondary-source-assessment.md)), agree scope with Kerri/Scott, revert |
| [O28](outstanding/O28-access-hospitality-epos-integration.md) | 2 | Access Hospitality EPOS integration feasibility — Maker's critical path (0/8) | Integrations / Access Hospitality EPOS | OPEN | Andy (technical) · Kerri + Scott (roadmap) | **If Access can't be reached, most of the proposition collapses** — it holds **9 data domains**: sales, budgets, comps/discounts/staff meals, waste, stock (weekly wet stocktake + deliveries + transfers + variance/GP), the full wet **and** food product list with hand-maintained prices, recipe/dish costing, PLU sales, cash discrepancies. 6 of the 9 map onto **existing** DV entities with no model change; **budgets and recipes are the two genuine gaps**. **Access is not on the current integration list** — Andy said so plainly to the prospect and framed the estimate honestly as two parts: *technical build* (fast, five integrations already follow the `_INIT`/`_DDL`/`_Staging`/`_Mapping`/`_Final` pattern) vs *vendor access* (unknown, and the actual risk) — *"most of that time is going to be down to the supplier."* ⚠️ **Do not contact Access until Bex has sponsored it** — Kerri owns that trigger and a client-initiated request lands very differently *"rather than just them thinking a random guy is trying to get in."* Note **budget extractability was asserted in-call as an assumption, not verified**. If this stalls, **S4 Labour is the faster first win** (we already have some API knowledge). Read [O7](outstanding/O7-xmse949-product-cost-matching.md)/[O10](outstanding/O10-threerocks-sales-double-count.md) before designing product/recipe cost handling, and [O25](outstanding/O25-d-discount-microservice-id-typing.md) before the discount lane |
| [O29](outstanding/O29-makers-secondary-source-assessment.md) | 3 | Maker's secondary sources: S4 Labour, Collins, Toggle, Wireless Social, Acteol (0/7) | Integrations / new source assessment | OPEN | Andy | ⚠️ **Two agreed rulings recorded here — apply at design time, don't widen later. (1) S4 Labour is headcount/hours/cost grain only, NO individual PII** — Andy raised the GDPR exposure unprompted and Bex agreed headcount suffices; **design to that grain from the start** rather than landing individuals and aggregating, per the Mews CRM-lane precedent where loaded PII had to be purged. The two per-individual ideas floated (team leaderboard: ATV + avg tip; staff-suitability flags — *"they're not a Sunday afternoon crew"*) are **explicitly out of scope** unless separately agreed. **Start with S4** — the only one of the five where we already have API knowledge (*"we've done a little bit of work with that"*), and labour % vs sales is a headline weekly metric. **Collins + S4 together unlock the flagship demo**: a booking spike the labour forecast hasn't accounted for, surfaced as an alert — the cross-source argument for the platform in one card. ⚠️ **No bookings source exists on the platform today** (no booking entity in the DV model), and **Wireless Social is a novel data shape** — nothing carries footfall, dwell or visit recency; it's guest-level, so the GDPR question applies at least as much. **Toggle is the cheapest credible win**: one small integration, one report (vouchers sold/redeemed/expired/**outstanding liability**), a total blind spot closed and a real year-end tax angle per Scott. **Acteol is lowest value** — dormant (one email Jan 2026, none since; social is their channel) — but it's an Access Group product so shares [O28](outstanding/O28-access-hospitality-epos-integration.md)'s commercial route, and could be the first real consumer of the **9 live-but-unmapped CRM entities** (verify that count against `docs/data-vault-reference.md` §6) |
| [O30](outstanding/O30-invoice-ingestion-price-drift.md) | 3 | Emailed invoice ingestion → supplier price-drift + duplicate-order detection (0/7) | Ingestion / flat-file + alerting | OPEN | Andy | **Do sub-task 1 first: the flat-file/email ingestion capability was described to a prospect as in-flight and its actual state is unverified in this repo.** The enabling fact is strong — **95–99% of supplier invoices already arrive by email** (paper + digital both exist; the team just use the paper copy on a clipboard), so the digital source is already there. Replaces a daily hand-keying loop that **isn't working**: continually ~£1,000/wk over food budget. Three use cases, all traceable to prospect statements: **(a) price drift** — *"I only know that chicken has moved 25p if the supplier has told me"*; ⚠️ **must be per product PER SUPPLIER** (milk is ordered from either of two suppliers depending on who delivers next day, so product-only comparison generates false positives), and ⚠️ **key off invoice/delivery date, not email receipt** (one supplier batches a week's invoices on a Monday). **(b) duplicate ordering** — the 5-litre mayonnaise ordered Tuesday *and* Wednesday *"because one of you didn't know the other one had ordered it"*; cheapest high-impact piece, **make it the demo**. **(c) purchase-frequency product ranking** — falls out free and solves the prospect's own stated fix for near-duplicate products (*"if I knew which was the most bought product, I could just pick that for the recipe"*), which also repairs stale recipe costs downstream. **Build generically if at all** — every inventory client has this blind spot; model on `F_PURCHASES_DAY` + the `STOCKEVENT` `ORDER` lane, don't invent a parallel purchase model, and surface alerts via the suggestion engine ([O3](outstanding/O3-suggestion-redesign-xmse948.md)) rather than a new mechanism. Gated on [O27](outstanding/O27-makers-of-hospitality-discovery.md) — requirement capture until the account converts |
| [O32](outstanding/O32-growyze-pantry-cogs-dashboard.md) | 2 | Growyze Pantry COGS dashboard — replaces a customer's two hand-maintained spreadsheets | Growyze / presentation + Report DB | OPEN | Andy | **Authored 2026-07-30; COMMITTED and FIRST-EXECUTED against UAT org 21 on 2026-07-31 — read-only, nothing deployed yet.** That first execution found a **new Critical (C4)** that four prior review passes could not: every prior session had no DB access. The build returned 732 rows in which **every money column was NULL** (732/732 `HAS_ZERO_COST`) because `ItemCost` filtered `SAT_INVITEM.EFFECTIVEFROM <= PERIOD_END_DATE`, and on a freshly-loaded org every satellite row carries the **load date** (2026-07-28) against periods ending 31 May / 30 Jun — zero matches. **All eleven checks passed or reported INFO on that empty fact:** check 5 is INFO by design so 100%-unpriced read as an expected shrug, and check 7 is a faithful **mirror** of the build's cost rule, so it resolved NULL too and its `ISNULL(x,0)-ISNULL(y,0)` came out zero — *a mirror check cannot falsify a premise it shares*. With `BarChartCard` rendering NULL as `0.00`, the runner would have declared success and shipped an all-£0.00 dashboard that looked deliberate. Fixed with a three-key `ORDER BY` fallback (keeps point-in-time intent, degrades to the nearest held cost, self-heals once real T2 history exists); check 7 updated **in lockstep** (else false FAIL on every priced row) and new **check 12** added as an *independent witness* that reads the fact directly and re-derives nothing — verify is now **12 checks / 13 rows**. **After the fix it reconciles exactly:** 31 May closing **£10,151.06** vs `F_INV_COUNTS_DAY`'s £10,175.54, and the £24.48 gap is that live fact **double-counting one item** (158 rows / 157 distinct items) — `10,175.54−24.48=10,151.06`, so **the new fact is the more correct of the two and exposed a pre-existing defect in a live one**. Prerequisite 2 **resolved** (report-DB org GUID = `67CA4E6F-9A7E-F111-B337-002248A1EC3D`, the GUID inside the MI database name). Two source caveats, neither a defect: this org sends **only COUNT and ORDER events**, so `WASTE_VALUE` is £0 and `VARIANCE_VALUE` = exactly `−COG_SOLD` — **the "unexplained shrink" KPI reports all consumption as unexplained**; and the first period sweeps 4,649 deliveries back to 2023 (correctly quarantined — every measure card excludes `IS_FIRST_PERIOD`), leaving org 21 with **one** usable period. 13 artefacts in `ClaudeDevelopment/integrations/Growyze/cogs/`: new `presentation.F_COGS_PERIOD` (item × location × **stocktake period**, **37 cols**, valued at *latest cost in period* so it reconciles with Growyze's own export), tier-110 build step, 15 vis queries on one dashboard, report-DB wiring, PowerShell runner, and an **11-check** `99_verify` written first. Reviewed: per-task → cross-file contract check → independent whole-pack review (3 Critical / 8 Important / 8 Minor) → fix wave → scoped re-review (19/19, 1 new blocker) → targeted round. **All 3 Criticals were plan/dispatch errors, each invisible from inside its own file** — config deployed to the wrong database (break-out silently did nothing while every check passed), a filter output contract no live `FilterList` uses, and the trend card collapsing to one month by default. Cross-linked to **Claude Nine O25** (commercial thread with Kati). **DEPLOYED to UAT org 21 on 2026-07-31**, steps 1–9 via `90_deploy_cogs.ps1`: **PASS=10 FAIL=0 WARN=1 SKIPPED=1 INFO=2** at step 9 (the real gate), every verdict exactly as predicted — **check 12 = PASS** (the new cost gate), check 11 = WARN (org 21 carries none of the configured break-out categories, so the break-out is *unproven* there, not broken), check 2 = SKIPPED, checks 5/6b = INFO. The deployed fact matches the pre-deploy simulation to the penny (732 rows / 732 distinct grain keys; June COG_SPEND £1,733.38, COG_SOLD £3,387.77, opening £10,151.06, closing £8,496.68) and zero-cost rows fell from 362-of-362 to **1** (the `All INVITEMs` sentinel). **Step 10 also DONE** — run by hand against `report` on `xms-sql-fog-uat`; its transactional verify returned **PASS: 12 cards, 3 filters, group-mapped and visible**, additive alongside O8's Marge Brut in the existing "All Dashboards" group (`BiConfig` already held `DbPrefix = 20260722`, independently confirming the prefix). ⚠️ the committed script keeps its placeholders **by design** — a substituted scratch copy was used, do not commit a pinned one. **Then verified by execution, not assumption:** all **15** dataset names and card types agree across the report DB and `core.core.VisualisationQueries` (the `DashboardGridFilter.DataSet` == `FilterDefinitions` key contract holds), and **all 5 card types return correct data** — COG Spend £1,733.38, COG Sold £3,387.77, Closing £8,496.68, pie £391.81+£2,995.96=£3,387.77 with zero slices omitted, bar £3,387.77, and the billing grid's **GRAND TOTAL £1,733.378404 exactly equalling the KPI** (check 3's three-way agreement proven through the real SPs). **C2 is settled by observation, not inference:** filtering COG Sold to `Food` returned the exact £2,995.96 subtotal and a deliberately **bogus venue hash returned NULL**, proving the filters genuinely apply rather than silently no-op — the precise failure mode the binary-vs-hex defect would have caused. `PERIOD_LABEL` renders `"Opening - 31 May 2026"`, not NULL. **Next: step 11, the browser check.** **ROLLED OUT 2026-07-31 to Padel Social (10) + Dirty Sixth (18)**, which surfaced a third Critical. **C5 — closing stock is a SNAPSHOT and was being SUMmed across periods.** Invisible on org 21 for the same reason C4 was invisible to eleven checks: that org has exactly **one** non-first period, so a sum and a snapshot are the same number. On Padel (33 period-ends × 2 locations) the Closing Stock KPI read **£709,650.42** against a true **£31,395.51** — **22.6× overstated as a headline figure**; Dirty Sixth ~5×. Four columns across two cards (the KPI plus the item table's `OPENING_QTY`/`CLOSING_QTY`/`CLOSING_VALUE`) now use the standard opening/closing-balance treatment — earliest period in scope for opening, latest for closing, resolved **per location** since venues keep independent stocktake calendars (D5/D6); flows stay plain SUMs. Verified: org 21 **unchanged £8,496.68** (no regression), Padel **£31,395.51**, Dirty Sixth **£16,860.51**, and the grid's Beverages root row **£9,402.39** (snapshot) not £59,677.98 (old sum). ⚠️ **Second defect of this exact shape** — correct on the first deploy org *because that org's data is degenerate*. Org 21 has one period, one location and no waste/sale events, so it cannot exercise period arithmetic, cross-location resolution or the flow/snapshot distinction: **it is not a sufficient acceptance environment**; shape-test against Padel. **Both orgs FAIL check 4** — 9 (Padel) and 12 (Dirty Sixth) Growyze items with no category landing on the `All INVITEMs` sentinel, worth **0.4%** and **0.3%** of value; the runner halted on it as designed and deployment continued past it deliberately after measuring. Fix belongs with Growyze. **Dirty Sixth is FULLY LIVE** (steps 1–10, report DB *PASS — 12 cards, 3 filters, group-mapped*). **Padel is now WIRED too** (report DB *PASS — 12 cards, 3 filters, group-mapped*, alongside its 4 existing dashboards) after O34's blocker was disproved. Wiring it immediately exposed **C6, the worst defect in the pack: the fact was APPENDING on every rebuild, not replacing.** Padel's Closing Stock KPI read **£62,791.01 — exactly 2×** the £31,395.51 verified minutes earlier; the fact held **35,112 rows for 17,556 distinct grain keys** and Dirty Sixth **14,264 for 7,132**. Neither was built twice by hand — **one scheduled presentation refresh did it**, since registering tier 110 globally means every org's scheduled rebuild now runs the step. Cause: `sp_ExecuteQuery` gates its pre-insert DELETE on `@TimeSeriesTargetColumn IS NOT NULL` but runs the INSERT **unconditionally**, and this step registered `time_series_target_column` as NULL — **the only Fact of 20 to do so** (bar `PF_GROWTH_PERIOD`). No error, no warning, and it **compounds every cycle**. Fixed to `PERIOD_END_DATE` in **both** MERGE branches (the UPDATE branch is what lets a re-run repair an already-broken row); all three orgs rebuilt clean and **idempotency proven by rebuilding twice in a row**. ⚠️ **Check 1 is the correct guard and still missed it, because the duplicate arrives AFTER deployment** — a deploy-time gate cannot see what the *next* scheduled rebuild introduces, so `DEPLOY.txt` now says re-run `99_verify` a day later. ⚠️ **`PF_GROWTH_PERIOD` (tier 102, parent-org) has the same NULL column and is likely accumulating the same way — check it under O4.** **One prerequisite left:** a Growyze COGS export in `[reference].[GROWYZE_COGS_EXPORT_STAGING]` (until then check 2 = SKIPPED, so faithful replacement of the spreadsheet is unproven — though closing stock now reconciles exactly against the platform's own fact). Expect **check 12 = PASS** (hard gate) and **check 11 = WARN** (org 21 carries none of the configured break-out categories). **Three decisions waiting** (comparison card can't target chosen months; check 11 vs `03`'s own docs; slow-movers spec deviations) — see detail file §Decisions. Expect **INFO** on checks 5/6b and **PASS-or-WARN** on 11; only FAIL halts. Do NOT reconcile against Padel Social or Dirty Sixth — implausible stock values from a separate cause (O5) |
| [O33](outstanding/O33-inv-counts-day-movement-fanout.md) | 1 | `F_INV_COUNTS_DAY` movement join fans out count rows, inflating closing stock value | Presentation / inventory facts | OPEN | Andy | **Blast radius MEASURED 2026-07-31 and far larger than the £24.48 single item that raised this: Padel 259 duplicate (location,item,date) groups, Dirty Sixth 56 with up to 3 rows each.** Critically it corrupts **`THEO_QTY`** (differs in 230 of Padel's 259 groups) while `ACTUAL_COUNT`/`UOM_COST` are repeated identically — so any `THEO_QTY` stock valuation is **non-deterministic** (same query returned £17,777.76 and £17,534 on consecutive runs; ±£868 band on one venue). O5's Tasks F/G now value `ACTUAL_COUNT` deduplicated to count grain. Org 21 shows 1 group and 0 `THEO_QTY` disagreements, so it cannot detect this. **Next: fix the movement join so it cannot multiply a count row; until then never value stock on `THEO_QTY`** |
| [O34](outstanding/O34-padel-biconfig-wrong-dbprefix.md) | 4 | Padel Social's report-DB `BiConfig.DbPrefix` names a database that has never existed — but is NOT load-bearing | Microservice report DB / org config + doc fix | DOC-DEBT | Andy | ✅ **The DB half is DONE (2026-07-31); only the doc correction remains.** ⚠️ **SEVERITY CORRECTED DOWN — the original claim here was wrong.** — it changes 4 dashboards outside O32's scope. `BiConfig` maps an org to its MI database as `{DbPrefix}_XMS_{OrganisationId}`; Padel Social holds `20251208`, resolving to `20251208_XMS_94A4B719-…`, which **is not on the server** — the real database is `20260310_XMS_94A4B719-…`. It is the **only mismatch of all 19 `BiConfig` rows**. `Audit.BiConfig` shows it was **wrong on the original INSERT** (2026-03-11, the day after the DB was created), not drifted — `20251208` is the prefix **three other UAT orgs legitimately share**, so it looks like a copy-paste; two later updates on 2026-07-03 never corrected it. The docs (§1/§11) say `DbPrefix` resolves the client database, which implied **Padel's 4 dashboards had never returned data**. That was flagged as *inferred, not observed* — and **Andy tested it: they render fine.** `InvCOGSByCategory` run against the **real** DB returns Beverages £35,573.16 / Other £10,731.42 / Food £3,985.27 / Uncategorised £3,519.59 / Retail £2,379.55, **matching the rendered dashboard exactly** (£56,189). So the FE resolves the client DB by some path that does **not** use `DbPrefix`; which path is unknown and needs the microservice source. **Nothing was ever blocked** — O32's Padel wiring was held on the strength of the wrong claim and the hold is lifted. ✅ **(a) THE ROW IS FIXED, 2026-07-31** — corrected `20251208` → `20260310` as step 1 of [O5](outstanding/O5-growyze-default-dashboards.md)'s Plan 3 deploy (`report_config/01_prereqs_biconfig_visconfig.sql`, a guarded UPDATE that no-ops on re-run) and independently re-verified through MCP. All **20** `BiConfig` rows now resolve to a database that exists — the Ibis Heathrow row created in the same deploy took the count 19 → 20. ⬜ **(b) the doc fix is all that remains:** `docs/microservice-report-database.md` §1 still asserts that `DbPrefix` resolves the client database, and §11 still lists `BiConfig` as the first result set of the card-render config load — a mechanism the running system demonstrably does not depend on. The doc should say what actually resolves it, or say plainly that it is unverified. Reusable lesson: docs + schema + a corroborating audit trail all agreed, and were still wrong about the running system — one look at the UI settled it. Worth the cheap Prod sweep noted in the detail file (join `BiConfig` to `sys.databases` on `DbPrefix + '_XMS_' + OrganisationId`) once Prod has orgs |
| [O35](outstanding/O35-mews-no-product-cost.md) | 2 | Mews populates no product cost — profit cards blank on Mews-only orgs | Data Vault / presentation — Mews cost path | OPEN | Andy | **Found while building O5's source precedence, and it is an ABSENT cost path, not a weak one — do not fold into O7.** `F_PRODUCT_MARGIN_DAY.AVG_NET_COST` is **100% NULL on every Mews row** (Ibis Gloucester 703/703, Oak & Vine 1,110/1,110), and `PROFIT` is NULL wherever cost is, so any profit or margin measure over Mews sales returns NULL. Consequence: `GrowyzeProfit` / `GrowyzeProfitPct` can **never** populate on a Mews-only org — Ibis Gloucester (21) and Ibis Heathrow (20) — and render blank; sales-only cards there are unaffected and correct. Surfaced only because precedence pointed those cards at Mews sales for the first time. **Distinction that matters:** [O7](outstanding/O7-xmse949-product-cost-matching.md) is the NCRAloha↔MarketMan cost-match resolving *poorly* (Oak & Vine's NCRAloha rows are 32% NULL — that **is** O7, and its `PRODUCT_COST_MAP` fix would address it); O35 is that **nothing maps a cost onto a Mews product at all**, which `PRODUCT_COST_MAP` as scoped would not fix. Worth knowing: **Marge Brut on the Ibis orgs does not hit this**, because it derives cost from Growyze stock movement rather than this fact — so a working cost figure for those hotels already exists in the inventory lane, and the two lanes currently disagree about whether cost is knowable. **Next:** confirm whether Mews staging/`SAT_LINEITEM` carries any cost column at all or the API simply returns none; then a product call — map cost from the inventory lane, or accept Mews as sales-only and give profit cards an explicit "no cost data" state, or suppress those cards on Mews-only orgs at the Plan 3 wiring step. ⚠️ One of the latter two **must** be decided before O5's Plan 3 wires the pack to the Ibis orgs, or those two hotels ship with two permanently blank cards |
| [O36](outstanding/O36-growyze-purchases-no-location.md) | 2 | Growyze purchase lines carry no location — deliveries not analysable per venue | Growyze staging / `F_PURCHASES_DAY` | OPEN | Andy | **Found 2026-07-31 building O5 Plan 2 Task B.** A planned `location <> 'Unknown'` guard silently destroyed **60% of Dirty Sixth's delivery value** (£27,554 vs £68,248, 1,160 of 2,141 rows). The dropped rows carry the -999 location sentinel and span the fact's whole range. Guard removed from `GrowyzeDeliveriesValue`; unattributed shares: Dirty Sixth 54% of rows, Oak & Vine / Gloucester 12%, Heathrow 9%, Padel 6%. **Per-venue delivery analysis is impossible for most of Dirty Sixth's spend until fixed.** Next: trace whether Growyze's payload carries a location per order line at all. Same shape as O23 |
| [O31](outstanding/O31-dining-covers-spend-per-head.md) | 4 | Dining covers + spend-per-head measure, and cover-count plausibility flagging (0/6) | Presentation / measures + data quality | OPEN | Andy | **The most articulate ask in the call, and BLOCKED on the prospect's own check** — Bex is verifying whether a seated walk-in is keyed with its true headcount or just `1`. The measure is one decision: **denominator = covers on food-bearing bills only**, because a bar-only drinker currently counts as a cover and dilutes SPH — which matters acutely across a 65–70% food site and a 60–70% wet site. Answers the question the monthly management accounts can't: when revenue is £10k down, **is that cover decline or SPH decline?** Andy's reusable judgement: the **calculation** is easy, the **capture** is the risk — this account is unusually tractable because the individual counts *are* trusted, whereas *"some operators, their method of capturing that cover count isn't the trustworthy bit, and then you're in a world of just fuzziness."* ⚠️ **Ruling: flag implausible counts (1 cover vs 4 mains) to managers; NEVER infer or overwrite a corrected headcount** — Andy advised against it in-call and Bex asked TR to make the case to the team directly (*"get the root of the problem sorted"*). Inferring covers from dish counts would manufacture a confident wrong number and destroy the measure's credibility — same trap as [O8](outstanding/O8-marge-brut-dashboard.md)'s NULL-rendered-as-`0.00` and [O23](outstanding/O23-growyze-supplier-bottom-level.md)'s supplier collapse. Also check the split-bill covers rule before touching cover arithmetic. Likely a **platform-wide** measure rather than bespoke |

---
| [O37](outstanding/O37-f-inv-daily-detail-uom-cost-null.md) | 3 | `F_INV_DAILY_DETAIL.UOM_COST` is 100% NULL — "Total Inv Cost" card blank on every org | Presentation / `F_INV_DAILY_DETAIL` | OPEN | Andy | **Found 2026-07-31 by O5 Plan 3's shared-dataset audit.** `F_INV_DAILY_DETAIL` is populated (11,549–113,412 rows/org) but `UOM_COST` is NULL on **every row of every org, all time** — so the LIVE `OakVineInvTotalCost` KPI (`'£' + FORMAT(SUM(UOM_COST),'N0')`) returns NULL and **renders blank on all 5 orgs**. It is already wired live on Oak & Vine, so this is a **pre-existing live defect**, not one O5 introduced. Two further problems if anyone repairs rather than retires it: `SUM(UOM_COST)` **sums a per-unit cost with no quantity** — dimensionally not a stock value even once populated — and it snapshots `MAX(BUSINESS_DATE)`, which holds only 5–262 rows per org. **O5 dropped the card from the pack** (Overview 9→8, Inventory 10→9); nothing in the pack now reads this fact. Next: (a) find why the build never populates `UOM_COST` — check whether the column is even in the `PresentationControl` step's INSERT list; (b) decide repair vs retire. If repairing, multiply by a quantity, and never by `THEO_QTY` (**O33**) |
| [O38](outstanding/O38-oakvine-invitem-orphan-counts.md) | 2 | 95% of Oak & Vine's inventory count rows have no `D_INVITEM` row — item-level cards show ~5% of the data | Presentation / `D_INVITEM` build | OPEN | Andy | **Found 2026-07-31 by O5 Plan 3's shared-dataset audit.** Of Oak & Vine's 19,185 `F_INV_COUNTS_DAY` rows, only **465 (Growyze) + 468 (MarketMan)** resolve to a `D_INVITEM` row; **18,252 (95.1%)** carry a real `INVITEM_HUB_ID` with no dimension row. **Not the sentinel** — `INVITEM_HUB_ID = CONVERT(BINARY(32),-999)` returns **0** there. The other four Growyze orgs return **0 orphans**, so Oak & Vine — the only one with two inventory sources — is the only affected org measured so far. Consequence: `InvUseAnalisys` inner-guards on `BOTTOM_INVITEM_NAME IS NOT NULL`, so on Oak & Vine that grid silently shows **933 of 19,185 rows**; `F_INV_USAGE_DAY` has the same shape (105,330 of 113,411 unresolved). **A grid showing 5% is far harder to spot than one showing none** — and it was invisible until now because the orphans are excluded by the *card*, so any check counting what a card returns sees a clean non-zero result. **Third failure of this class on this workstream** (O23 suppliers, O36 purchase locations, now inventory items) — check for a shared upstream cause in the dimension build, starting with a `BOTTOM_LEVEL = 1` anchor in the `Inventory Item Dimension` step. Verify any fix on Oak & Vine; the others cannot detect it |
| [O39](outstanding/O39-margebrut-branch-unmerged.md) | **1** | ~~O23's fix~~ **THREE branches carry UAT-live work that `main` lacks; `origin/main` is 34 behind** | Repo hygiene / release integrity | OPEN | Andy | 🔺 **ESCALATED to Pri 1 on 2026-08-04 by O5 Plan 4.** This was raised about **one** branch and is really about **three**, measured: `worktree-growyze-dashboards-o5` **+7** (Plan 3 *and* Plan 4 — both live on UAT), `worktree-margebrut-live` **+12** (O23 + Marge Brut — live on UAT), `feature/v1.0-baseline-refresh` **+26** (O32 Pantry COGS + MDM registry). `worktree-growyze-uom-cost` is the only one merged. **And `origin/main` is 34 commits behind local `main` — nothing has been pushed at all.** So a v1.1 cut from `main` today would omit Plan 3, Plan 4, O23 and Marge Brut, while UAT keeps passing because UAT already has them. Also note O5's pick-up notes carried a stale **"the branch is merged"** claim — true when written on 2026-07-31 (the FF took `main` to `d175d30`), wrong the moment Plan 3 landed. **A merge note dates instantly; re-measure with `git rev-list --count main..<branch>` rather than trusting one.** Next: merge `worktree-margebrut-live`, then `worktree-growyze-dashboards-o5`, review each with `git log main..<branch>` (~45 commits total), sort the `origin/main` push, then sweep every remaining branch. ── Original entry: **Found 2026-07-31 by O5 Plan 3, while checking whether a script number was free.** [O23](outstanding/O23-growyze-supplier-bottom-level.md)'s supplier `BOTTOM_LEVEL` fix (`Growyze/22`, `23`, runner `94`) was **deployed to UAT 2026-07-30 and is live there**, but those files exist **only on `worktree-margebrut-live`** — not on `main`, not on any other worktree (`git branch -a --contains 99f0a14` returns that branch alone). **The repo's main line does not contain a fix that is running in UAT.** Three consequences: (1) **v1.1 cut from `main` as it stands would silently omit it**, and since the fix is already live on UAT, UAT would keep passing while Prod got a version missing it — the environment you test in being *ahead* of the artefact you ship is the worst shape of this problem; (2) **numbers 22/23/94 are invisibly taken** — `git ls-tree` on other branches shows the folder ending at `21`/`93`, so the next author reaches for `22`; O5's Task 1c hit this and moved to `24`; (3) **O8's Marge Brut work is on the same branch** and is *live and verified* on two UAT orgs, so it is in the same position — this is not just three Growyze files. **Next: merge `worktree-margebrut-live` (review it properly — `git log main..worktree-margebrut-live`) BEFORE O5's release-prep cuts `releases/v1.1`; also sort the `origin/main` push, which is still behind local `main`; then sweep every worktree branch for the same defect with `git log main..<branch>`.** General lesson: "deployed to UAT" and "in the repo" are independent facts — the ledger records the first, `git` the second |
| [O40](outstanding/O40-frontend-null-headline-and-legend-rendering.md) | 3 | Front end renders NULL headlines as `0.00`, drops legend swatches, shows join-key columns | Dashboard front end | OPEN | Andy → FE | **Raised 2026-08-04 by O5 Plan 4's formatting audit. These have now been diagnosed as SQL work TWICE** — by Friday's browser test (O5.S10) and by the Claude Design review (`requiresSql` items 1, 4, 12, 14) — and they are **not** SQL. That is the reason the item exists. **D1:** `InvStockActivity`'s header already returns `NULL AS Value`; the card renders `0.00` above the chart anyway, and the *same dataset* as a StackedBar renders `0` — so it is not one shared formatter. The doc's proposed fix ("return NULL so the frontend suppresses it") **is already the state**, making it unactionable; the only SQL option left would be to invent a meaningless headline. A headline of `0` also makes "no data" indistinguishable from "genuinely zero", which bites hardest on the orgs where Growyze carries almost no waste. **D2:** the MultiLineChart legend renders as an unswatched vertical text list even though the query emits `LegendLabel` as literals on every row of all three series. **D3:** `CustomGroupedDataGrid` renders `ParentId`/`Id` — neither query *labels* them; they are card-contract columns supplied to drive parent/child grouping. Recommendation: suppress the headline entirely when the header `Value` is NULL, and never render `ParentId`/`Id`. O5.S12 (heatmap 7-row axis) is related but **moot** — Plan 4 removed that card from the pack |

## Closed

*(none yet — when an item is confirmed done, move its row here with a date and one-line outcome, and keep its detail file for history)*

---

## Maintenance

- **One detail file per item** under `outstanding/O<n>-<slug>.md`. When you advance a task, append a dated entry to that file's **Progress log** and update its status mini-table — then sync the `Status` / `Next action` cells in the table above.
- **Adding an item:** add a table row with the next `O<n>` ID and create its detail file from the `task-detail.md` template (status mini-table → context → Progress log → Pick-up notes).
- **Never mark an item done / Closed without explicit user confirmation** — even when the work looks finished and verified. Report it and ask first; only then move its row to the **Closed** section above.
- Per-script status (test results, target stack) belongs in `ClaudeDevelopment/QUERY_STATUS.md`, not here; this board tracks *work*.
- The session-start hook injects this whole file (table only). Keep it table-only — task detail belongs in the per-item files, not here.
